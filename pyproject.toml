[tool.ruff]
line-length = 120
target-version = "py310"
exclude = ["tools"]

[tool.ruff.lint]
select = ["E", "F", "W", "I"]
ignore = ["E501"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["context_server/tests"]
